# MetaStack — Handoff for the Next Change

Paste-ready context to continue MetaStack in a new chat. Discovery and project-init are **done** — do not re-run them.

## Where things are
- **Repo:** `github.com/bijeded/metastack` (public). Local: `/Users/franciscovenegas/Desktop/Cowork/Metastack`
- **Live app:** https://metastack-three.vercel.app (auto-deploys on merge to `main`; preview per PR, but previews are Vercel-auth-protected — open while logged in)
- **Source-of-truth docs:** `CLAUDE.md` (stack, commands, conventions, deploy, design tokens) and `openspec/project.md`
- **Living specs:** `openspec/specs/metagame-breakdown-view/` and `openspec/specs/metagame-data-pipeline/`
- **Design system:** `design/` (tokens in `design/tokens/`, reference components, `design/MetaStack.dc.html` prototype). Theme wired via `src/styles/tokens.css`; dashboard layout in `src/styles/dashboard.css`.

## What's shipped (all changes archived under `openspec/changes/archive/`)
1. **`view-metagame-breakdown`** — pick a format → top-20 archetype breakdown, format persisted in `?f=`, "Updated X ago" freshness, loading/empty/error states, ES/EN.
2. **`add-metagame-filters`** (2026-07-01) — a **time-frame filter** in a real full-height filter sidebar: pick a window (originally three; **narrowed to two — Last 5 Days / 2 Weeks — in #8**), persisted in `?w=`, default Last 5 Days, heading "Time Frame"/"Periodo", with a `≡` toggle + mobile drawer. Breakdown + freshness are per (format, window). Backed by a window-aware schema and a scraper that fetches those windows per format.
3. **`view-archetype-decklists`** (2026-07-02) — expand an archetype card into its recent decklists (4 most-recent Top-4 finishes, else latest 4), a decklist modal (main/sideboard, dismissible, focus return), and **MTG Arena export** from the modal: Standard/Pioneer copy Arena text to the clipboard (localized confirmation, graceful failure); Modern/Pauper/Pre-Modern download a `.txt` (button reads "Download Decklist" for those). Export text leads with an `About`/`Name <archetype>` block and prefers Scryfall canonical name + non-foil printing when present, else the scraped name. Backed by new `events`/`decks`/`deck_cards` tables (RLS read-only), MTGTop8 event + decklist parsers, incremental + staggered per-format scraping, and a 6-month prune. Specs: `archetype-decklists-view` (new) + `metagame-data-pipeline` (extended).
4. **`add-scryfall-mapping`** (2026-07-02) — a **Scryfall bulk-data sync + card mapping** so `deck_cards.scryfall_name/set_code/collector_number` are populated (were always null). `scraper/scryfall.py` downloads the `default_cards` bulk file once/day (date-keyed cache, `actions/cache` shared across the staggered per-format scrape jobs) and resolves a scraped name → canonical non-foil paper printing, handling split/DFC names incl. MTGTop8's single-slash form (`Fire / Ice`) and preferring a standard printing over promo/Universes-Beyond variants. The writer enriches new cards at scrape time; a one-time `python scraper/run.py --backfill-scryfall` (service-role) mapped the existing rows (**41,053 of 41,054** — the miss is `Embiggen`, a silver-border card). No schema change (columns already existed). Specs: `scryfall-card-mapping` (new) + `metagame-data-pipeline` (extended).
5. **`add-card-art`** (2026-07-02) — **real Scryfall card art** hotlinked from the CDN. `deck_cards.image_url` + `archetypes.signature_card_name`/`art_image_url` (nullable). The resolver reads `image_uris.normal` (front-face fallback); the writer stores the deck-card image, and a signature-card pass sets each archetype's most-played non-land card + art (basics-only land exclusion — a type-based one is a follow-up). Frontend: a `CardArtPreview` shows a card's full art on **hover (mouse) / touch (mobile)** of its name in the decklist modal (portal, lazy, viewport-clamped, `onError` fallback, no layout shift); **ArchetypeCard** renders the signature-card art (cover) with the gradient as fallback. The `--backfill-scryfall` pass now uses `image_url` as the completeness sentinel (re-enriches identity-mapped-but-imageless rows) and also refreshes archetype art; backfill populated **41,053** deck-card images + **325** archetype arts. Specs: `card-art-display` (new) + `scryfall-card-mapping` / `metagame-data-pipeline` (extended).

6. **`refine-signature-card-selection`** (2026-07-02) — a **truer archetype signature card**. `deck_cards` now stores `type_line`/`rarity`/`cmc`/`released_at` and `archetypes.art_crop_url` (all nullable, additive schema). Land exclusion is now **type-based** (any `type_line` containing "land", basic + nonbasic — replaces the old basic-name list), and non-land candidates are ranked by total quantity → rarity (mythic>rare>uncommon>common) → set release date → cmc → name, with null metadata sorting last (resolved beats unresolved at equal quantity). `refresh_archetype_art` now also writes `art_crop`, and the **ArchetypeCard** prefers `art_crop_url` (falls back to normal image, then gradient). A one-time `python scraper/run.py --backfill` (service-role) fills the new columns on existing rows (keyed on the `type_line` null sentinel) and recomputes signature cards + art. Specs: `metagame-data-pipeline` / `scryfall-card-mapping` / `card-art-display` (extended).

7. **`add-scryfall-remap`** (2026-07-02) — a one-time **`--remap-scryfall`** maintenance pass that re-resolves **all** existing `deck_cards` rows against the current resolver (no null-sentinel filter, unlike `--backfill-scryfall`/`--backfill`), so a *later* resolver/printing/metadata heuristic change reaches already-enriched rows. For each distinct name that resolves it rewrites all eight Scryfall columns; a **miss is skipped** (existing data is never nulled). Standalone (does not scrape), recomputes archetype art after, and is exposed as a `remap-scryfall` `workflow_dispatch` option. No schema change. Run it **only after a resolver change** (`gh workflow run scrape.yml -f format=remap-scryfall`). Specs: `scryfall-card-mapping` / `metagame-data-pipeline` (extended).

8. **`scrape-full-two-week-window`** (2026-07-03, Change 1 of a scraper-logic fix) — a **complete, correctly-scoped deck corpus**. The decklist scraper now follows **every page** of a window's events list (`&cp=2`, `&cp=3`, … until a page yields no new events, capped at ~20; a `cp` param on `format_url`/`fetch`), instead of only page 1. Scope narrows to the **two-week window** (`DECKLIST_WINDOWS`; last-5-days is a date subset), the tracked logical windows drop to `5days`/`2weeks` (dropped `2months` from `WINDOWS`/`WINDOW_META`, the UI selector, and both ES/EN locales), and **retention drops 6 months → 30 days** (`RETENTION_DAYS`). No schema migration — the `meta_window` CHECK stays permissive and stale `2months` snapshot rows are inert (both removed by the follow-up). New page-2 + empty-page fixtures. Specs: `metagame-data-pipeline` / `metagame-breakdown-view` (modified). **Follow-up (now shipped — see #9): `derive-metagame-from-decks`.**

9. **`derive-metagame-from-decks`** (2026-07-04, Change 2 of the scraper-logic fix — the pivot) — the metagame breakdown is now **derived frontend-side from our own scraped decks** instead of MTGTop8's pre-aggregated breakdown, fixing wrong archetype names and high-share-but-empty cards (the two naming systems no longer disagree). One `useMetagame(format, window)` hook fetches the window's decks once and produces both the breakdown (group by archetype → share = count/total, top 20) and the per-archetype drill-down, so every shown archetype has ≥1 deck; per-archetype display count 4 → 6. Freshness is now **per-format** via `formats.last_updated_at` (`useLastUpdated` reads it by `code`; the scraper stamps it after each successful format scrape). The scraper's breakdown pass is gone (`pipeline.py`, `parse_meta_breakdown`/`rank_archetypes`, `replace_breakdown`/`stamp_updated` removed). The schema **drops `metagame_snapshots` + `format_window_freshness` + the `meta_window` column** (idempotent `drop … cascade`; the archetype-dedupe block reworked to canonical-by-lowest-id; validated with `pglast`) — applied manually with the service-role key after merge. Safe merge order was frontend (#62) → scraper (#63) → schema (#64) → this closeout. Specs: `metagame-breakdown-view` / `metagame-data-pipeline` (modified).

10. **`refine-printing-selection`** (2026-07-03) — the Scryfall resolver now prefers a **plain** printing over any special-treatment variant. `_is_special_printing` in `scraper/scryfall.py` was broadened beyond promo/Universes-Beyond to also demote `full_art`, `textless`, `border_color == "borderless"`, and `showcase`/`extendedart`/`inverted` frame effects (non-borderless borders — black/white/silver/gold — stay eligible); a new `_set_type_tier` prefers `expansion`/`core`/`masters` over `commander`/`draft_innovation`; and `_selection_key` now ranks `(plain, set_type_tier, released_at, set_code)` so **plain treatment beats both recency and set type** (a plain old printing wins over a borderless new one). Fixes wrong alternate art (e.g. Opt, Stock Up) in **both** decklist card images and archetype signature-card art — one fix, since both resolve through the shared `CardIndex`. Scraper-only, no schema/frontend change; closed out with the standard one-time `--remap-scryfall` run. Specs: `scryfall-card-mapping` (modified).

11. **`add-power-score-tiers`** (2026-07-04) — a **performance-based Tier badge + a recent-window trend arrow**, so tiers reflect *win capability*, not popularity (the old fixed share cutoffs measured popularity). A new pure `src/lib/powerScore.ts` scores each archetype from its deck **placements**: a bracket **finish-quality** map (1→1.0, 2→0.8, 3-4→0.65, 5-8→0.45, 9-16→0.3, 17-32→0.18, ≥33→0.1; leading-int bucketed, so `"3-4"` and bare Swiss `"12"` map alike), the **Wilson lower bound of the *mean* quality** ×100 as a small-sample-robust Power Score (*mean* not sum ⇒ volume can't buy score; no hard floor — the Wilson shrink alone tames tiny samples), and **Fisher–Jenks natural breaks** (not fixed thresholds) to cut a field into T1/T2/T3/Otros. `src/lib/metagame.ts` gains `attachPowerTiers` (a pure orchestrator): the badge is the archetype's **2-week** Power Score classified against the **2-week top-20** Jenks reference field, so it is **stable across the 5days⇄2weeks toggle**. The window signal is surfaced instead as an **arrow-only `TrendIndicator`** (▲/▼/–, ported from the design `ChangeIndicator` with the *number suppressed*, localized `trend.*` aria-labels) shown **only on Last 5 Days**, comparing the selected window's **raw mean finish-quality** (NOT the shrunken score) to the 2-week baseline, with a 0.02 deadband + a 3-deck minimum guard; **null (no arrow) on the 2-week baseline view**. `useMetagame` now fetches the **2-week corpus once** and derives the selected window by a **client-side date filter** (2 weeks ⊇ 5 days), so both the stable tier and the trend come from one query. **Metagame share % is unchanged**; the share-based `tierFor` is **retired** (`TierBadge`/`ArchetypeCard` take an explicit tier). **No schema/scraper/dependency change, no history table.** Verified read-only against live Supabase across all five formats (T1 archetypes finish deeper than Otros; low-share strong performers out-tier high-share weak ones). Specs: `metagame-breakdown-view` (modified — 6 tier/trend requirements added).

12. **`add-metagame-filters`** (2026-07-06) — **Event + Archetype filter groups** in the existing sidebar, deriving everything client-side from the already-fetched decks (no schema/scraper change). The **Event** filter is a single-`<select>` ("All events" default + one option per event in the current (format, window), labelled name + abbreviated date) that restricts the breakdown/decks to that event — so each archetype's **percentage is recomputed *within the event*** (sums to 100%). The **Archetype** filter is display-only: selecting one **collapses the grid to that card and auto-expands it with *all* its decks** (cap lifted), so it does **not** distort the event-share %. Filters **AND** with the time-frame; selections are **in-memory only** (no URL params); an invalid selection (after a format/window/other-filter change) **silently auto-resets** to "All"; a selected archetype with no decks shows a localized empty state. Both per-group "All" defaults and a global **Clear filters** button. `useMetagame` gained an `eventId` filter param, an `events` option list, and `fullDecksByArchetype` (uncapped, date-desc then placement); when **event-filtered**, expanded cards also use the full list (best-finish-first). New `EventSelector`/`ArchetypeSelector`/`ClearFiltersButton` mirror `WindowSelector`; shared `formatShortDate` extracted; `filters.*` keys + a locale-parity test. Specs: `metagame-breakdown-view` (modified — 5 filter requirements added).
13. **`add-archetype-win-trophy`** (2026-07-06) — a **🏆 win trophy** on archetype cards. `deriveBreakdown` now tallies a `wins` count per archetype (decks whose finish is exactly 1st, via `placementBadge(...).kind === 'first'`), computed from the **same window/event-filtered deck list** as the share, so the trophy tracks the active filters. A reusable **`WinTrophy`** component renders a bare 🏆 for one win and **🏆 ×N** (mono multiplier) for more, nothing at 0, inline **after the archetype name** (smaller font; `minWidth:0` on the name so it still ellipsizes while the trophy stays pinned). Follows the `TrendIndicator` a11y pattern (`role="img"` + localized count-aware `wins.*` aria-label — "N event wins" / "N victorias"; glyphs `aria-hidden`). The 🏆 is a **deliberate, scoped exception** to the "no emoji" design rule — **recorded in `CLAUDE.md`** and the spec. No schema/scraper/dependency change; Power-Score tiers unchanged (they already fold in 1st places — the trophy is additive). Verified live (Standard: Izzet Prowess 🏆×3, single-win archetypes a bare 🏆). Specs: `metagame-breakdown-view` (modified — Archetype win trophy requirement added).

14. **`add-stat-card-strip`** (2026-07-06) — a **right-aligned header StatCard strip** showing **Events · Archetypes · Decks** for the currently displayed metagame. `useMetagame` gained a `totals: { events, archetypes, decks }` computed over the same window+event-filtered deck set as the breakdown (so it narrows under an event filter), with **`archetypes` as the distinct total (uncapped)** — independent of the grid's top-N slice, so it stays honest at ≥ cap. The design's `StatCard` was ported to `src/components/StatCard.tsx` (mono value per the "all data is mono" rule + `toLocaleString` thousands separators; `stats.*` i18n). `App` renders the strip `marginLeft:auto` in the header title row (title/pill/freshness unchanged) and **special-cases the display-only archetype filter**: when an archetype is selected the strip shows Archetypes = 1, Decks = that archetype's decks, Events = the distinct events among them (keyed name+date, since `DeckRow` has no event id). Verified live (Standard: unfiltered 7·19·90; Izzet Prowess filter → 4·1·16). Specs: `metagame-breakdown-view` (modified — Header StatCard strip requirement added).

15. **`retune-breakdown-cap-and-tier-field`** (2026-07-07) — **retuned the breakdown cap + tier field, uncapped the archetype filter, and added a Tier filter** (frontend-only, no schema/scraper). In `src/lib/metagame.ts` the overloaded `TOP_N=20` split into an exported **`GRID_DISPLAY_CAP=12`** (a *display* cap applied by the caller) and an **uncapped `deriveBreakdown`** (optional `cap` param); the Fisher–Jenks **tier reference field is now the whole 2-week corpus** (uncapped), so every archetype — not just a top slice — is tiered (`useMetagame` renamed `PowerContext.twoWeekTopNames` → **`twoWeekFieldNames`** and emits the full tiered `breakdown`; the grid slices to 12 in `App`). The default popularity grid shows the top 12 with a localized count-aware caption **"Top N most popular archetypes"** above the freshness line (`dashboard.topCaption_one/_other`). The **Archetype filter dropdown is uncapped** (fed from the full `breakdown`). New **`TierSelector`** (mirrors Event/Archetype selects) after the Archetype filter: All tiers / Tier 1 / Tier 2 / Tier 3 / **Rogue-Otros** (maps to T1/T2/T3/**Otros**; `filters.tiers`/`allTiers`/`tierLabel` + the shared `tiers.rogue` label). Selecting a tier shows **all** that tier's archetypes as **collapsible** cards (uncapped, click-to-expand), replaces the caption with a **tier caption** in the same spot (`dashboard.tierCaption_one/_other`, e.g. "Tier 1 — 13 archetypes"), **ANDs with the event filter**, and **narrows the StatCard strip** to the tier (via a shared `totalsFromDecks` helper). A tier matching nothing shows the localized empty state (tier options are **static**, so — unlike Event/Archetype — the tier is **not** auto-reset for absence; its only reset is the **archetype-precedence** rule: isolating an archetype outside the selected tier silently drops the tier). Clear filters + format change reset the tier. Verified live read-only across all five formats: monotonic performance-based tiers, decoupling confirmed (below-top-12 archetypes reach ≥T2). **Calibration note (deferred):** large formats show a broad T1 (PAU 24, MO 17) from single-deck event winners — a `Z_DEFAULT` (Wilson z) tuning is a separate follow-up, not this change. Specs: `metagame-breakdown-view` (modified — display cap/caption, tier field, uncapped archetype filter + Tier filter/precedence/empty-state requirements).

16. **`event-scoped-grid-caption`** (2026-07-08, fast mode) — the **default popularity grid becomes event-aware** when an event is selected (frontend-only, no schema/scraper). In `src/App.tsx` a shared **`eventLabel`** (selected event's name + abbreviated date via the shared `formatShortDate`, name-only when dateless; `null` when no event) drives two changes: (1) in the default popularity view (no archetype isolated, no tier), a selected event **uncaps** the grid (`visibleBreakdown` uses the full `breakdown` instead of `.slice(0, GRID_DISPLAY_CAP)`), and (2) the caption above the freshness line reads the **event name + date** (e.g. "Standard Challenge — 24 Jun 2026") instead of "Top N most popular archetypes". When a **tier and an event** are both selected, the tier caption **combines** them via a new `dashboard.tierEventCaption` (`"{{tier}} — {{event}}"`, ES/EN) — e.g. "Tier 1 — Standard Challenge — 24 Jun 2026". No-event popularity/tier captions and the isolated-archetype view (caption still hidden) are unchanged; share % is untouched (already recomputed within the event). Event names stay English in both locales (proper nouns). Specs: `metagame-breakdown-view` (modified — Event filter uncap + event-name caption; Tier filter event-combined caption).

17. **`show-bare-position-above-top-8`** (2026-07-08, fast mode) — a **placement-label tweak** so a bare Swiss standing deeper than 8th reads as the raw number instead of a fake bracket (frontend-only, no schema/scraper/i18n). `placementBadge` in `src/lib/placement.ts` gained one branch: `nums.length === 1 && high > 8` returns `{ label: String(high), kind: 'other' }` — so `"9" → 9`, `"14" → 14`. **Bracket ranges** are unchanged (`"5-8" → Top 8`, `"9-16" → Top 16`, `"17-32" → Top 32`; they have two parsed ints), and single standings of 8th or better keep `Top {n}` (`"8" → Top 8`), alongside `1st`/`2nd`/`Top 4`. Label-only: the badge `kind` (colour) is untouched, and both surfaces (archetype-card deck rows + decklist modal) update at once since they share `placementBadge`. Competitive labels stay English in both locales. Specs: `archetype-decklists-view` (added — "Placement result label format" requirement).

18. **`scrape-twice-daily`** (2026-07-08, fast mode) — the scraper now runs **twice a day** instead of once (config/docs-only, no code/schema). `.github/workflows/scrape.yml` gained a second staggered per-format band: a **morning band at 10:00–11:00 UTC** and an **evening band at 22:00–23:00 UTC** (same 15-min `ST/PI/MO/PAU/PREM` stagger), and the `Resolve format` `case` grew 5 → 10 arms in **lockstep** with the cron list (an unmapped cron still fails the run loudly). The bands target **~6:00 AM / 6:00 PM UTC-6** (fixed offset, no DST) but are scheduled **~2 h early on purpose** to compensate for GitHub's variable cron delay (scheduled runs fire ~2–4 h late on this repo — verified against Actions history: a 12:00 UTC schedule started 14:07–16:17 UTC across recent days, which is why the old "6 AM" run was seen ~8 AM). Actual times still drift within ~a 2 h window — documented in a workflow comment so nobody "corrects" it back to 12:00/00:00. Workflow name + `daily-scrape-` concurrency prefix unchanged (no Actions-history churn). Scryfall date-keyed cache (both bands share a UTC day → ~1 download/day) and the idempotent 30-day prune are unaffected. Specs: `metagame-data-pipeline` (modified — twice-daily two-band schedule). **Gotcha recorded:** GitHub Actions schedules crons late (~2–4 h) and variably; off-peak minutes reduce it, and top-of-hour/:00/:15/:30/:45 marks are the worst.

19. **`group-decklist-by-card-type`** (2026-07-08) — the **decklist modal's mainboard is grouped by card type** into four fixed-order sections — **Lands → Creatures → Spells → Other** — each with its own summed count (frontend-only, no schema/scraper; `deck_cards.type_line` was already populated by the Scryfall mapping). A pure `src/lib/cardType.ts` classifies a card from its Scryfall `type_line` by precedence — contains "Land" → Lands (so a Land Creature like Dryad Arbor is a Land); else "Creature" → Creatures (Artifact/Enchantment Creature counts); else Instant/Sorcery/Enchantment → Spells; else (incl. a **null** type_line — a Scryfall miss) → Other — plus `groupMainByType` (buckets in fixed order, preserving input order within each). `useDeckCards` now selects `type_line` and exposes `typeLine?: string | null` on `DeckCardLine`. `DecklistModal` renders a localized, subordinate `GroupHeading` (+ mono count) per **non-empty** group inside the existing `.decklist-main-cols` two-column flow (`break-inside/after: avoid` on the heading so it isn't orphaned); **empty groups render no heading**. The **sideboard stays a single flat list** and the **MTG Arena export is byte-for-byte unchanged** (`buildArenaDeck` still gets the flat `main` array — grouping is display-only). New `modal.group.lands|creatures|spells|other` keys (ES: Tierras/Criaturas/Hechizos/Otros) with parity coverage. Verified live: Selesnya Aggro → Lands 22 · Creatures 29 · Spells 8 · Other 1; UW Control → Lands 23 · Spells 11 · Other 26 with the Creatures heading correctly hidden. Specs: `archetype-decklists-view` (modified — mainboard card-type grouping + localized headings).

20. **`derive-color-identity-from-cards`** (2026-07-08) — archetypes whose **name carries no color** (named for a card/mechanic, not a guild/shard/mono/letter code) now get a **card-derived WUBRG color identity** instead of a lone gray pip (scraper + one-time backfill; **no schema/frontend change** — `archetypes.color_identity` and the pip rendering already existed). The Scryfall resolver now carries `color_identity` on `Printing`/`CardIndex` (from the bulk rows). A pure `color_identity_from_decks(deck_color_sets)` in `scraper/mtgtop8.py` returns the WUBRG-ordered union of colors appearing in **≥ `COLOR_IDENTITY_MIN_DECK_SHARE` (0.35)** of an archetype's decks (a **deck-presence** threshold, so a one-off splash is dropped). A new `SupabaseWriter.refresh_archetype_color_identity(fmt)` uses the **name-derived identity when present, else the card-derived value** (`_card_color_identity` groups mainboard `deck_cards` by deck, unions each deck's cards' Scryfall colors), and **change-detected PATCHes `color_identity` only** (idempotent, recomputed each run — not insert-only). Wired into the per-format scrape loop and the shared maintenance helper (renamed `_refresh_all_archetype_art` → **`_refresh_all_archetype_derived`**, now runs art **and** color), plus a standalone **`--refresh-color-identity`** mode (+ `refresh-color-identity` `workflow_dispatch` option) for the one-time backfill. Backfill run live (Actions `format=refresh-color-identity`): recolored **ST 2 / PI 14 / MO 66 / PAU 67 / PREM 0** archetypes; verified name-derived correct and card-derived sensible on real samples (PI Arclight Phoenix → UR, PAU Affinity → UBR). **Color identity is now name-first, cards-fallback** (updates the old "derived from the archetype name" gotcha). Specs: `metagame-data-pipeline` (modified — Archetype color identity from name, then cards). **Deferred:** on **tiny (2-deck) archetypes a single-deck splash can leak** (a pure *share* threshold means one of two decks = 0.5 ≥ 0.35, e.g. PAU Bogles → WUG on 2 decks); the fix is a **minimum absolute deck-count floor** (a color must appear in ≥2 decks), a scoped `color_identity_from_decks` follow-up — kept `0.35` since base colors are correct on real samples. Archetypes with **no decks in the current window stay empty** (derivation needs decks; e.g. MO Arclight Phoenix is empty in MO but UR in PI).

21. **`weight-power-score-by-tournament-size`** (2026-07-08) — **tournament-size weighting + Tier-1 calibration** (deferred #7), a cross-stack change sequenced schema → scraper → frontend so every intermediate `main` was safe. Schema: additive nullable **`events.player_count`** (`add column if not exists`, pglast-validated). Scraper: `parse_event_size(html)` reads the "<N> players" figure from the event page's `class=S14` meta panel (the pipeline fetches the event page once and reuses it for both size and deck parsing — no extra request), and `upsert_event` writes `player_count` **only when known** so a missing size never nulls a stored one (PostgREST merge-duplicates). Frontend (`powerScore.ts`/`metagame.ts`/`useMetagame.ts`): each finish is weighted by its event size (`sizeWeight`, `[0.35, 2.5]` around `SIZE_REF 64`; null→0.35 small-event floor, never dropped) and the Power Score is the Wilson lower bound over an **effective n = Σ weights**; raised **`Z_DEFAULT` 1.5→2.0** and added a **`T1_MIN_DECKS`=3** floor (below-floor archetypes drop to **T2**, not fringe). Share %, StatCard totals, trending, decklists unchanged; the **trend stays unweighted**. Verified live read-only across all five formats: **PAU T1 24→8, MO 17→14**, monotonic tiers, no T1 below the deck floor, graceful degradation with all-null sizes. Specs: `metagame-data-pipeline` + `metagame-breakdown-view` (modified). See the updated Power Score gotcha. **Backfill of existing events** is handled by the follow-up **`add-tournament-size-backfill`** (#22).

22. **`add-tournament-size-backfill`** (2026-07-08, fast mode) — a one-time **`--backfill-sizes`** scraper mode that fills `events.player_count` on already-stored events (the incremental scraper skips them, and #21 only records size on new events, so existing rows stayed null and size weighting was uniform). `SupabaseWriter.events_missing_size` cursor-pages the null-size events; `backfill_event_sizes` (in `decklist_pipeline.py`, injected fetcher) re-fetches each event page, runs the existing `parse_event_size`, and `set_event_size` PATCHes `player_count` when found (a miss stays null; decks/cards untouched; one event's failure doesn't abort). Standalone (no Scryfall, no decklist scrape), exposed as the `backfill-sizes` `workflow_dispatch` option. **Run it once post-merge:** `gh workflow run scrape.yml --ref main -f format=backfill-sizes`, then re-verify the tier distribution now reflects real sizes. Specs: `metagame-data-pipeline` (modified). 6 new scraper tests (136 total).

Backed by a twice-daily MTGTop8 scraper → Supabase (RLS read-only). 293 frontend + 136 scraper tests.

## Stack & commands (see CLAUDE.md for full detail)
- Frontend: React 19 + Vite 8 + TS 5.8, Vitest (config in **separate `vitest.config.ts`**), oxlint, react-i18next, @supabase/supabase-js, Recharts (still unused).
- Scraper: Python 3.12 + requests + BeautifulSoup4 (venv at `scraper/venv`); no `supabase-py` (raw PostgREST via requests).
- Commands: `npm run dev` | `npm run build` | `npm run test` | `npm run type-check` | `npm run lint`; scraper: `cd scraper && ./venv/bin/pytest`.
- CI required check name is **`ci`**. Branch protection is active on `main` — everything lands via PR; the assistant cannot self-merge without an explicit "merge" / "squash and merge".

## Data model (Supabase — schema is manual, `supabase/schema.sql`)
- `formats(code PK, name, last_updated_at)` — codes ST/PI/MO/PAU/PREM. `last_updated_at` is the **per-format freshness source of truth**: the scraper stamps it after a successful run and the frontend reads it for the "Updated X ago" indicator.
- `archetypes(id, format_code FK, name, color_identity, signature_card_name, art_image_url, art_crop_url, unique(format_code,name))` — one per format; the scraper upserts (get-or-create), never deletes per-run. `signature_card_name`/`art_image_url`/`art_crop_url` (nullable) are the archetype's most-played non-land card + its hotlinked Scryfall images, recomputed each run. A functional `unique(format_code, lower(name))` index additionally blocks case-variant duplicates.
- **No `metagame_snapshots` / `format_window_freshness` tables** — dropped in `derive-metagame-from-decks`. The metagame breakdown is **derived at read time** from the decks (grouped by archetype within the window's date range); freshness is per-format via `formats.last_updated_at`.
- `events(id, source_event_id, format_code FK, name, event_date, player_count, unique(source_event_id, format_code))` — a MTGTop8 event. `player_count` (nullable) is the tournament size, parsed from the event page when MTGTop8 reports it (else null); it weights the performance Power Score.
- `decks(id, event_id FK, archetype_id FK, source_deck_id, player, placement, unique(event_id, source_deck_id))` — one deck at an event. `placement` is raw text ("1", "3-4", "5-8") because `placing` is a reserved SQL word.
- `deck_cards(id, deck_id FK, board 'main'|'side', quantity, card_name, scryfall_name, set_code, collector_number, image_url)` — one card line; `scryfall_*` + `image_url` are **populated** by the Scryfall mapping (scrape-time + one-time backfill); still nullable, and a resolution miss (e.g. silver-border cards) stays null → export falls back to `card_name`, art to the placeholder. `image_url` is the backfill's completeness sentinel.
- RLS: anon SELECT only on all tables; writes only via service-role key.

## Workflow to follow (Implementation mode: disciplined)
1. New session in the repo → optionally **user-stories** skill → **`/opsx:propose <change-id>`** (pass the stories as context).
2. **task-execution** skill, one task group per branch: TDD (tests first) → spawn a subagent **code-review** (clean context) → conditional **security-review** subagent for sensitive surfaces → PR via **github-pr** → **human merges**, then check off tasks and pull `main`.
3. When all tasks done → **`/opsx:sync`** deltas into `openspec/specs/` → **`/opsx:archive`** (both land via a `chore:` PR since `main` is protected).
- Branch `task/<n>-<desc>`; commit `<task-id>: <imperative>`; PR bodies end with the Claude Code footer + `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- **Tip:** when a change spans a shared contract across schema + scraper + frontend, either bundle it into ONE PR, or **sequence the PRs so every intermediate state is safe**. `derive-metagame-from-decks` removed `meta_window` + the snapshot tables by ordering frontend → scraper → schema (each layer stops reading the old thing before the next stops writing it), so `main` was never broken across four PRs.

## Gotchas learned (save time)
- **Time windows are format-independent LOGICAL keys** (`5days`/`2weeks`), NOT raw MTGTop8 params. The frontend applies them as **client-side date filters** over the decks (and derives the breakdown from those decks); there is **no `meta_window` column** anymore (dropped with the snapshot tables in `derive-metagame-from-decks`). **MTGTop8's numeric `meta` IDs are per-format** — same window, different ID per format — so the scraper still maps `(format, window) → meta ID` via `WINDOW_META`/`meta_id_for` in `scraper/mtgtop8.py`. Hardcoding one format's IDs for all formats silently stores identical data (the bug that time-frame verification caught). The decklist pass gathers only `2weeks` (superset of `5days`) and follows all events-list pages (`&cp=N`). "2 Months"/"Large Events"/"MTGO" are not tracked.
- **`window` and `placing` are reserved SQL keywords** — that's why the live `decks.placement` column isn't named `placing` (and why the dropped `meta_window` wasn't `window`); quoting an identifier is a cross-stack footgun.
- **Global CSS only applies if imported in `src/main.tsx`.** `src/index.css` is orphaned Vite-template leftover and does nothing — layout classes live in `src/styles/dashboard.css` (imported by main.tsx). A class-based layout that "does nothing" is almost always this.
- **Schema deploy dance:** applying `supabase/schema.sql` needs the **service-role key** (a human/CI step; the assistant only has the anon key). After merging a schema+scraper PR: apply the schema in the Supabase SQL editor, then `gh workflow run scrape.yml --ref main` to repopulate. `schema.sql` is idempotent (safe to re-run); validate SQL locally with **`pglast`** (the real PostgreSQL parser — it DOES catch reserved-keyword-as-identifier errors, unlike `sqlglot`).
- **Printing selection prefers the PLAIN treatment.** `scraper/scryfall.py` picks one printing per card by `_selection_key = (plain, set_type_tier, released_at, set_code)` — plain treatment (not promo/crossover/`full_art`/`textless`/`borderless`/showcase/extended/inverted) beats set-type tier (`expansion`/`core`/`masters` > neutral > `commander`/`draft_innovation`) beats recency. Only `border_color == "borderless"` demotes — black/white/silver/gold borders are fine. **Any change to this heuristic needs a `--remap-scryfall` run** to reach existing rows.
- **Color identity is name-first, cards-fallback** (as of `derive-color-identity-from-cards`). Primary signal is the archetype **name** (guild/shard/mono/explicit letters) via `scraper/mtgtop8.py:color_identity_for` — MTGTop8 exposes no mana symbols. When the name yields **nothing**, the identity is **derived from the decks' cards' Scryfall `color_identity`** (`color_identity_from_decks`, kept if a color is in ≥ `COLOR_IDENTITY_MIN_DECK_SHARE`=0.35 of the archetype's decks) by `SupabaseWriter.refresh_archetype_color_identity`, recomputed each run (idempotent, change-detected PATCH). Needs decks to derive — an archetype with **no decks in the window stays gray**. Any tuning of the share constant / a min-deck floor only touches `mtgtop8.py`; re-run the one-time `--refresh-color-identity` (Actions `format=refresh-color-identity`) to reach existing rows.
- **Power Score = performance; its Tier badge is 2-week-anchored (stable) and tournament-size-weighted.** `src/lib/powerScore.ts` scores an archetype from deck **placements** (bracket finish-quality → **size-weighted** **Wilson lower bound of the *mean* quality** → **Fisher–Jenks** tiers). Each finish is weighted by its event's **`player_count`** via `sizeWeight` (clamped `[SIZE_WEIGHT_MIN 0.35, SIZE_WEIGHT_MAX 2.5]` around `SIZE_REF 64`; a **null/unknown size → the 0.35 small-event floor**, never dropped), and the score uses **Σ weights as the effective Wilson `n`** (bigger/more events ⇒ less shrink). A **`T1_MIN_DECKS`=3 floor** bars an archetype with too few supporting decks from Tier 1 (it drops to **T2**, not the fringe) so single-tiny-event winners don't flood T1. `archetypePowerScore(placements, sizes?, z)` — omitting `sizes` gives neutral weight 1 (pure unit tests); production passes a per-finish sizes array parallel to placements (`useMetagame` builds `twoWeekSizes` alongside `twoWeekPlacements`). The badge always uses the **2-week** score against the 2-week Jenks field, so toggling `5days`⇄`2weeks` never changes it; the recent-window signal is the separate **▲/▼/– trend arrow** (`TrendIndicator`, only on Last 5 Days). The trend compares **raw mean quality** between windows — **NOT** the Wilson-shrunken score, and stays **UNWEIGHTED** by size (`windowTrend` takes placements only), so it reflects a real performance change, not which tournaments were in the window. `useMetagame` fetches the **2-week corpus once** and date-filters client-side. Calibration constants (`Z_DEFAULT`=2.0, `SIZE_REF`/`SIZE_WEIGHT_MIN`/`SIZE_WEIGHT_MAX`, `T1_MIN_DECKS`, `TREND_EPS` 0.02, `MIN_TREND_DECKS` 3) sit at the top of `powerScore.ts`; property tests don't depend on exact values. Share % is untouched. **Note:** existing events have `player_count` = null until a re-scrape backfills sizes (the incremental scraper skips already-stored events), so weighting is uniform (all-small) until then — the tier improvement so far is from the `Z`/floor change; sizes refine T1 further once populated.
- **Tests:** jsdom has no `matchMedia` — it's stubbed in `src/test/setup.ts` (the sidebar drawer needs it). Scraper tests use saved fixtures under `scraper/tests/fixtures`, never live network.
- Transient CI `npm ECONNRESET` happens occasionally → `gh run rerun <id> --failed`.
- `.env.local` holds `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` (set locally and in Vercel) — useful for read-only anon verification via `curl` against the PostgREST API.

## Deferred work → candidate next changes
1. ~~Type-based land exclusion for the archetype signature card~~ — **DONE** in `refine-signature-card-selection` (2026-07-02): exclusion is now type-based and selection ranks by quantity→rarity→set-recency→cmc→name.
2. ~~Re-map existing rows on heuristic changes~~ — **DONE** in `add-scryfall-remap` (2026-07-02): a one-time `--remap-scryfall` mode re-resolves all rows (no null sentinel) and skips misses, so future resolver changes reach existing data. Run it (only after a resolver change) via Actions → `scrape.yml` → format `remap-scryfall`. **This is now the standard post-merge step for any future resolver/heuristic change.**
3. **True week-over-week delta (▲/▼)** — `add-power-score-tiers` shipped an **arrow-only `TrendIndicator`** (ported from the design `ChangeIndicator`) for *window-over-window* momentum (Last 5 Days vs the 2-week baseline) **without a history table**. A genuine **week-over-week** delta (this week vs last week, or a stored previous share/score) still needs history — the schema is current-only — and remains deferred; the component is ready to reuse. Same for a meaningful trend on the **2-week baseline view** (currently no arrow there).
4. ~~More filters: event + archetype~~ — **DONE** in `add-metagame-filters` (2026-07-06): single-event select (recomputes share within the event) + archetype isolate/auto-expand, ANDed with the time-frame, in-memory only, silent auto-reset, per-group + global clear. (Event-size stays dropped — MTGTop8's size/scope windows aren't uniform across formats.)
5. **~~StatCard strip~~ + "En Tendencia" trending table** — the **StatCard strip is DONE** in `add-stat-card-strip` (2026-07-06). The **trending table** is still open, but cheaper than this note originally assumed: after the derive-from-decks pivot, "top cards" is **derivable at read time from `deck_cards`** (card_name/quantity/image already populated — no new scraping), and a week-over-week ▲/▼ delta is **derivable from a wider deck fetch** (two adjacent 7-day slices within the 30-day retention — the decks *are* the history), same client-side pattern as the existing `TrendIndicator`. The one real design question is **client-side `deck_cards` volume** (decks × ~75 lines): pull-and-aggregate in JS vs. a Supabase view/RPC — settle with a `count(*)` spike before proposing. Trending metric semantics (total copies vs. deck-inclusion rate; main vs. main+side) also TBD.
6. ~~`retune-breakdown-cap-and-tier-field`~~ — **DONE** (2026-07-07, see #15 above): grid cap 20→12 with a caption, tier field decoupled to the whole 2-week corpus, uncapped Archetype filter, and a new Tier filter. Verified live across all five formats.
7. ~~Tier calibration — `Z_DEFAULT` / small-sample shrink~~ — **DONE** in `weight-power-score-by-tournament-size` (2026-07-08, see #21 below). Raised `Z_DEFAULT` 1.5→2.0 and added a `T1_MIN_DECKS`=3 floor (below-floor archetypes drop to T2, never the fringe), **plus** tournament-size weighting. Live re-verify: PAU T1 24→8, MO 17→14, monotonic, no T1 below the deck floor.
8. **Card-derived color identity — small-sample splash floor** (surfaced during `derive-color-identity-from-cards`'s live verification, 2026-07-08) — the card-derived fallback uses a pure **deck-presence *share*** threshold (`COLOR_IDENTITY_MIN_DECK_SHARE`=0.35), so on **tiny (2-deck) archetypes a single off-color deck (0.5 share) leaks a splash pip** (e.g. PAU Bogles → WUG on 2 decks). Raising the share won't fix it (a legit base color is also only at 0.5 there). The clean fix is a **minimum absolute deck-count floor** in `color_identity_from_decks` (a color must appear in **≥2 decks**, not just the share), so single-deck splashes never count while base colors survive. Scope: `scraper/mtgtop8.py` (`color_identity_from_decks`) + tests, then re-run the one-time **`--refresh-color-identity`** (Actions `format=refresh-color-identity`) to reach existing rows. No schema/frontend change.

## To start the next change
Open a new chat here and say, e.g.:
> *"Read docs/HANDOFF.md. Let's implement the already-proposed `fix-archetype-name-casing` change (disciplined mode). Don't re-run propose."*

— or pick any deferred item above (for a brand-new change: run **user-stories** then **`/opsx:propose <id>`**). Discovery/project-init are done; don't re-run them.
